# Copy to .env (repo root) — used by docker compose when building the citizen
# app image. These are public client-side values (same ones as in
# apps/citizen/.env.local for local dev).
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
